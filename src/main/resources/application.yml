spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/consulteme
    hikari:
      maximum-pool-size: 50
  jpa:
    hibernate:
      ddl-auto: update
  data:
    mongodb:
      database: demo
      username: admin
      password: password
      port: 27017
      host: localhost
      authentication-database: admin
