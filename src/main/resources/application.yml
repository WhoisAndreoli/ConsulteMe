spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/consulteme
  jpa:
    hibernate:
      ddl-auto: update
  data:
    mongodb:
      database: demo
      username: admin
      password: password
      port: 27017
      host: localhost
      authentication-database: admin
